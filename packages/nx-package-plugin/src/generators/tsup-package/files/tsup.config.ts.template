import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm", "cjs"],
  dts: true,
  sourcemap: <%= tsupOptions.sourcemap %>,
  clean: true,
  splitting: <%= tsupOptions.splitting %>,
  treeshake: true,
  target: "node18",
  outDir: "dist",
});