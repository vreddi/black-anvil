{
  "generators": {
    "tsup-package": {
      "factory": "./dist/generators/tsup-package/tsup-package",
      "schema": "./dist/generators/tsup-package/schema.json",
      "description": "Generate a TypeScript package configured to use tsup as its bundler"
    }
  }
}
